import React from 'react';
import dialogs from './Dialogs.module.css';

const Dialogs = (props) =>{
    return(
        <section>
            Dialogs;
        </section>
    );
}

export default Dialogs
